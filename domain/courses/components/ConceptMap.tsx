'use client'

interface ConceptMapProps {
  courseId: string
}

export default function ConceptMap({ courseId }: ConceptMapProps) {
  return (
    <div>
      {/* Concept map will be implemented here (Phase 2) */}
    </div>
  )
}
