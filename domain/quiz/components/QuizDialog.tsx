'use client'

interface QuizDialogProps {
  postId: string
  open: boolean
  onClose: () => void
}

export default function QuizDialog({ postId, open, onClose }: QuizDialogProps) {
  return (
    <div>
      {/* Quiz dialog will be implemented here (Phase 2) */}
    </div>
  )
}
