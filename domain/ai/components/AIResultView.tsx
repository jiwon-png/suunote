'use client'

interface AIResultViewProps {
  result: {
    summary?: string
    keyPoints?: string[]
    studyDirection?: string
  }
}

export default function AIResultView({ result }: AIResultViewProps) {
  return (
    <div>
      {/* AI result view will be implemented here */}
    </div>
  )
}
